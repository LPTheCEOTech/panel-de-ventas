node_modules
.next
out
build
.DS_Store
*.pem
npm-debug.log*
.env*.local
.vercel
*.tsbuildinfo
next-env.d.ts

# secretos del proyecto — NUNCA se versionan
.db-password
.login-password
.vercel-token
.mcp.json
supabase/.temp/

# andamiaje de la SaaS Factory / agentes IA — NUNCA se versiona en este repo.
# Si querés usar la Factory en tu copia local, funciona igual; solo no se sube.
.claude/
CLAUDE.md
CLAUDE.*.md
